/**

@page README
@section About

	libchessengine - Parse PGN files, make moves from an game in input PGN file, print game, move, table to stream like std::ostream ( for example std::cout )
					 Uses modified pgnlib for parse PGN files

@section pgnlib Library
	Compile, decompile chess games from/to *.pgn files.
	@ref http://en.wikipedia.com/PGN_Notation
	Uses GNU GPL library. Original source: http://sourceforge.net/projects/pgnlib

@section _inst_ Installation Chess engine
	@note Install libpgn first (@ref _lpgninst_), then instal libchessengine
	make <p/>
	sudo make install
	@subsection _lpgninst_ libpgn Installation
		./configure
		make
		sudo make install
@section Using
		First @ref _inst_
		This library can be build as static library and/or shared object
		@see examples

@example _exs_ libchessengine

@brief First example

@code main.cpp

#include<iostream>
#include<CEEngine.h>
int main()
{
	ChEngine::Engine engine;
	engine.open("games.pgn");
	std::cout<<engine<<std::endl;
	return 0;
}
@endcode

g++ main.cpp -lchessengine -o myapp

	or 

g++ main.cpp -lchessengine -static -o myapp
*/
