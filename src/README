/**

@page README
@section About

	libchessengine - Parse PGN files, make moves from an game in input PGN file, print game, move, table to stream like std::ostream ( for example std::cout )
					 Uses modified pgnlib for parse PGN files

@section _pgnmlib_ Library
	Compile, decompile chess games from/to *.pgn files.
	@see http://en.wikipedia.com/PGN_Notation
	Uses GNU GPL library. Original source: http://sourceforge.net/projects/pgnlib

@section _inst_ Installation Chess engine
	@note Install libpgn first ( @ref _lpgnminst_ ), then install libchessengine:
	make </p>
	sudo make install
	@subsection _lpgnminst_ libpgnm Installation
		./configure
		make
		sudo make install
@section Using
		First @ref _inst_
		This library can be build as static library and/or shared object

		See examples for more information.

@example examples/main.cpp

@brief First example


This example shows howto open pgn file and make all moves for each game from file.

Compile and run:

$ make <p>
$ make run

*/
